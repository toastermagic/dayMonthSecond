import {describe, ddescribe, expect, iit, it} from '@angular/core/testing';
import {Auth0profile} from './auth0profile.model';

describe('Auth0profile', () => {
  it('should create an instance', () => { expect(new Auth0profile()).toBeTruthy(); });
});
