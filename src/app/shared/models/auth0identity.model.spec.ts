import {describe, ddescribe, expect, iit, it} from '@angular/core/testing';
import {Auth0identity} from './auth0identity.model';

describe('Auth0identity', () => {
  it('should create an instance', () => { expect(new Auth0identity()).toBeTruthy(); });
});
